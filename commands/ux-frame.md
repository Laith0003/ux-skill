---
description: Moved to /ux-discover --frame. Alias kept until 4.1.
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
disable-model-invocation: true
---

# /ux-frame (alias)

`/ux-frame` moved to `/ux-discover --frame` in 4.0: the four-field framing block. This alias is removed in 4.1.

1. Tell the user in one line: "/ux-frame is now /ux-discover --frame. Running it."
2. Run `/ux-discover --frame $ARGUMENTS`. Follow `commands/ux-discover.md` in frame mode, with the same arguments, exactly as if the user had typed the new command.
