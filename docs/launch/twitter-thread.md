# Twitter / X thread (copy verbatim)

> 4 tweets. Schedule for Tuesday morning ET, same window as the Show HN post. Pin to profile after publishing.

---

## Tweet 1: hook

AI design output has fingerprints.

I catalogued 30 of them and built a Claude Code plugin that refuses every one.

Inter as the brand face. Purple-to-blue gradients. Three equal cards. "John Doe". h-screen on mobile.

Open source. Show HN today.

🧵

---

## Tweet 2: proof (attach a screenshot of the landing hero or a 5-sec screen recording)

The landing is the proof.

The plugin's own discipline produced this: Cormorant slab + cream canvas + coral accent + live /ux-design code-window mockup typing real output.

Built with 18 slash commands, 5 sub-agents, 72 brand DESIGN.md specs.

uxskill.laithjunaidy.com

---

## Tweet 3: example output (attach a screenshot of the dark code-window OR use the text below)

$ /ux-design "a SaaS landing for a fintech for freelancers"

avoided  Inter as display          → Geist
avoided  purple-to-blue gradient   → single Electric Blue accent
avoided  3 equal cards             → 7/5 split + zig-zag
avoided  centered hero             → asymmetric
avoided  generic placeholder names → Lina, Bashar, Adnan

Every command runs this self-review before shipping.

---

## Tweet 4: CTA + tags

17 more commands like that. 5 sub-agents (frontend-engineer, motion-engineer, copy-writer, research-synth, design-system-architect).

Install in any Claude Code session:

/plugin marketplace add https://github.com/Laith0003/ux-skill.git
/plugin install ux@ux-skill

Star if it helps:
https://github.com/Laith0003/ux-skill

cc @AnthropicAI @simonw @theshawwn @shadcn

---

## Optional follow-up tweets (reply to thread, post over 24-48h)

### Follow-up A: the bans list (visual)
Attach a screenshot of `references/foundations/anti-patterns.md` opened in a code editor.

Caption: "The full bans list. 30 deterministic rules with detection regex, severity, and a 'do instead' for every one. Lives in references/foundations/anti-patterns.md."

### Follow-up B: the brand library
Attach screenshots from 3-4 brand DESIGN.md specs.

Caption: "Say 'make it in Stripe's style' or 'match the Linear aesthetic' and the plugin reads the brand's actual DESIGN.md. 72 brands ready. Apple, Stripe, Linear, Figma, Tesla, Coinbase, Airbnb, Shopify, BMW, Ferrari..."

### Follow-up C: the linter
Attach a terminal recording of /ux-lint running.

Caption: "There's also a regex-only linter. No LLM, no API key, runs in ms. Exits non-zero on Critical/High. Wire it into pre-commit and your CI never ships AI slop."
